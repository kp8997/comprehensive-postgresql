1. 5 types of CONSTRAINTS
    PRIMARY KEY
    FOREIGN KEY
    NOT NULL
    UNIQUE
    CHECK

2. Fallback 
    default

3. 4 ways to to add constraint
    At creation
        table level
        column level
            add constain: foreign, primary, check, unique
            set not null

    After created
        table level
        column level
            add constain: foreign, primary, check, unique
            set not null

4. 
