Understand the select query that will return 3 kinds of dataset
    1. many rows, many column

    -- select * from users;

    2. many rows, one column

    -- select id from users;

    3. one row, one column (scalar query)

    -- select id from users where id = 1;

    -- select COUNT(*) from users;
